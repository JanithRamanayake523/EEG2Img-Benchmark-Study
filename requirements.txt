# ============================================================================
# EEG2Img-Benchmark-Study: Requirements File
# ============================================================================
#
# INSTALLATION INSTRUCTIONS:
#
# 1. Create Conda environment:
#    conda create -n eeg2img python=3.10
#
# 2. Activate environment:
#    conda activate eeg2img
#
# 3. Install PyTorch WITH GPU SUPPORT - Choose your CUDA version:
#
#    For CUDA 13.1 (Recommended for newer GPUs):
#    conda install pytorch torchvision torchaudio pytorch-cuda=13.1 -c pytorch -c nvidia
#
#    For CUDA 12.1:
#    conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia
#
#    For CUDA 11.8 (Older GPUs):
#    conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
#
#    For CPU only (no GPU):
#    conda install pytorch torchvision torchaudio cpuonly -c pytorch
#
# 4. Install remaining dependencies from this file:
#    pip install -r requirements.txt
#
# 5. Verify GPU support:
#    python -c "import torch; print('GPU Available:', torch.cuda.is_available())"
#
# ============================================================================

# Core scientific computing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# EEG processing
mne>=1.5.0
mne-bids>=0.13.0

# Time-series to image transforms
pyts>=0.13.0
PyWavelets>=1.4.0

# Deep learning - INSTALL VIA CONDA (see instructions above)
# torch>=2.0.0  (Install via conda with GPU support)
# torchvision>=0.15.0  (Install via conda)
# torchaudio>=2.0.0  (Install via conda)
timm>=0.9.0  # For pretrained ViT models

# Machine learning utilities
scikit-learn>=1.3.0
imbalanced-learn>=0.11.0

# Statistical testing
statsmodels>=0.14.0
pingouin>=0.5.4

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Data management
h5py>=3.8.0
pyarrow>=12.0.0

# Experiment tracking
wandb>=0.15.0
tensorboard>=2.13.0

# Configuration management
pyyaml>=6.0
omegaconf>=2.3.0

# Testing
pytest>=7.3.0
pytest-cov>=4.1.0

# Utilities
tqdm>=4.65.0
joblib>=1.3.0

